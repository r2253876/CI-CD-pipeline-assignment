# Fail `npm install`/`npm ci` outright if the running Node.js version does
# not satisfy package.json's "engines.node" — belt-and-braces alongside the
# OPA/Conftest policy (policy/node_version.rego) that checks the SAME pin
# against the Dockerfile's FROM line in CI, before an image is even built.
engine-strict=true
