apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-sample-api.fullname" . }}
  labels:
    {{- include "devops-sample-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-sample-api.selectorLabels" . | nindent 4 }}
