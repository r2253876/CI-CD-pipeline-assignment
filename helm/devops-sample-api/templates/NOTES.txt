devops-sample-api ({{ .Release.Name }}) deployed to namespace {{ .Release.Namespace }}.

Check rollout status:
  kubectl rollout status deployment/{{ include "devops-sample-api.fullname" . }} -n {{ .Release.Namespace }}

Get the ALB hostname once the AWS Load Balancer Controller provisions it:
  kubectl get ingress {{ include "devops-sample-api.fullname" . }} -n {{ .Release.Namespace }}

Check HPA status:
  kubectl get hpa {{ include "devops-sample-api.fullname" . }} -n {{ .Release.Namespace }}

Rollback to the previous revision:
  helm rollback {{ .Release.Name }} 0 -n {{ .Release.Namespace }}
